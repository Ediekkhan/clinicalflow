# Follow-up TODOs — 2026-07-16

## Database and deployment
- [x] Add support for cloud database configuration via environment variables.
- [x] Add `DATABASE_URL` override support to backend config and runtime.
- [x] Document how to run with local SQLite and how to switch to a hosted database.
- [x] Evaluate free-tier cloud database providers for PostgreSQL or SQLite-compatible hosting.
- [x] Use Neon PostgreSQL as the shared hosted database and local SQLite for isolated development; use Render for the free pitch backend.
- [x] Add README/guide entries for local and hosted DB usage.

## Backend portal wiring
- [x] Audit frontend portal routes and map each to a backend endpoint. See `PORTAL_API_AUDIT_2026-07-16.md`.
- [x] Create a portal audit document that lists which portals are fully wired, partially wired, or placeholder-only.
- [x] Implement missing backend endpoints for:
  - `/api/v1/specialist/*`
  - `/api/v1/hospital/*`
  - `/api/v1/clinic/*`
  - `/api/v1/pharmacy/*`
  - `/api/v1/lab/*`
  - `/api/v1/nurse/*`
  - `/api/v1/hmo/*`
  - `/api/v1/moh/*`
  - `/api/v1/admin/*`
- [x] Replace placeholder `EntityDashboard` API responses with authorized persistent or derived operational data.
- [x] Confirm hospital login and hospital-specific profile requests work correctly.
- [ ] Audit the signup / role-selection UI against actual portal wiring and backend auth.

## Authentication and session
- [x] Add persistent auth/session support instead of demo-only auth.
- [x] Wire hospital login to real backend authentication.
- [x] Add token/session refresh handling for portal API requests; full route/role protection remains in T06.
- [x] Validate that frontend auth uses the configured `NEXT_PUBLIC_API_BASE_URL` and `.env.local`.

## Neo4j / Knowledge Graph
- [x] Define a Neo4j schema and data model for the deterministic medical brain.
- [x] Use Neo4j as an auxiliary, global clinical-ontology service; PostgreSQL remains the transactional system of record.
- [x] Add a read-only backend service wrapper with a conservative deterministic fallback.
- [x] Document the graph model and its integration with symptoms, conditions, specialties, urgency, and persisted tickets. See `KNOWLEDGE_GRAPH_2026-07-16.md`.
- [x] Replace the placeholder-only Neo4j configuration with an opt-in, tested integration.

## Testing / validation
- [x] Add browser-level Playwright tests for patient, specialist, hospital, and PIN login plus booking, escalation, and scheduling.
- [x] Add regression tests for portal auth and queue endpoints.
- [x] Add documentation for local execution, hosted database switching, deployment, backup/restore, and operational controls.
- [x] Verify the frontend build with env loading after config changes. See `BASELINE_VALIDATION_2026-07-16.md`.

## Guardrails and operational edge cases
- [x] Implement forced overtake support in backend ticket escalation, audit logging, idempotent replay, and WebSocket broadcast.
- [x] Add a nurse portal forced overtake button and optimistic critical-column animation state.
- [x] Add WhatsApp/SMS intake intercept logic to detect duplicate phone-based queue entries.
- [x] Implement a shared-phone account grouping workflow for family members using the same number.
- [x] Add an interactive menu response path for WhatsApp/SMS duplicate identity cases.

## Unified appointment scheduling engine
- [x] Design and implement a shared appointment scheduling loop across WhatsApp, SMS, and web intake.
- [x] Implement provider slot lookup in PostgreSQL for live availability.
- [x] Add WhatsApp channel slot menu generation and interactive booking options.
- [x] Add SMS fallback text confirmation, cancellation, and low-bandwidth scheduling messages.
- [x] Add a web scheduling grid with persistent drag/drop rescheduling, lock, and emergency block controls.

## Interface viewport implementation
- [x] Build the public marketing website and live ROI calculator on `/`, including persisted demo capture.
- [x] Build the patient booking workspace on `/book` with slot picker, natural-language intake, persistent appointment booking, and confirmation receipt.
- [x] Build the patient live queue ticket viewport on `/my-visit` with shared-phone toggle.
- [x] Build the WhatsApp conversational triage interface canvas.
- [x] Build the SMS fallback text transmission engine output templates.
- [ ] Build the secure hospital auth gateway on `/auth/login` with role buttons and PIN pad.
- [x] Build the live nurse triage kanban dashboard on `/nurse/queue` with critical/urgent/routine lanes.
- [x] Build the patient context slide-over drawer component.
- [x] Build the master appointment scheduler and provider calendar grid on `/dashboard/appointments`.
- [x] Build the system admin configuration panel and hospital runtime controls, including the audited intake pause and channel routing.
- [x] Build the public waiting room TV viewport at `/hospital/waiting-room` (`/dashboard/waiting-room` redirects) with live ticket display and TTS.

## Edge resilience UI states
- [x] Add optimistic UI caching states for network transitions.
- [x] Add sticky network state toast for connected, reconnecting, and offline modes.
- [x] Add pending sync UI badge states for nurse actions during offline mode.
- [x] Ensure nurse card actions update client state immediately while syncing to the backend.
