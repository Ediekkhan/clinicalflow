import { EntityDashboard } from '@/components/entity/EntityDashboard';

export default function LabCollectionsPage() {
  return <EntityDashboard entity="lab" view="collections" title="Sample Collection" subtitle="Walk-in and home-collection schedule with collection officer assignment." />;
}
