import { EntityDashboard } from '@/components/entity/EntityDashboard';

export default function SpecialistSettingsPage() {
  return <EntityDashboard entity="specialist" view="settings" title="Specialist Settings" subtitle="Manage profile, credentials, consultation settings, notifications, and security." />;
}
